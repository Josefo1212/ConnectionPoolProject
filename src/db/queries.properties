# DEPRECATED: este archivo ya no es el principal.
# PostgreSQL usa: db/queries-postgres.properties
# MySQL usa: db/queries-mysql.json
#
# Se mantiene solo por compatibilidad temporal.
usuario.selectOne=SELECT * FROM usuario LIMIT 1

