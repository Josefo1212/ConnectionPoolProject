# Database configuration
DB_HOST=localhost
DB_PORT=5433
DB_NAME=javaprueba
DB_DIALECT=postgres
DB_USER=postgres
DB_PASSWORD=jf121206

# Driver/URL JDBC (pool genérico)
DB_DRIVER=org.postgresql.Driver
# Opcional (si lo pones, el Pool lo usará en vez de armarlo con HOST/PORT/NAME):
# DB_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}

# Configuración del pool de conexiones
POOL_SIZE=50
POOL_TIMEOUT=3000
